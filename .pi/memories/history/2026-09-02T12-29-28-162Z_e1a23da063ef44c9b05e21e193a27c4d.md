---
schema_version: 1
id: "e1a23da063ef44c9b05e21e193a27c4d"
state: "final"
outcome: "partial"
trigger: "prompt"
created_at: "2026-09-02T12:29:28.162Z"
started_at: "2026-09-02T12:29:28.162Z"
finalized_at: "2026-09-02T12:38:53.224Z"
session_id: "01a06217-d96c-7a71-9ad7-a3a1b6f4273d"
scope: "project"
root_history_id: "e1a23da063ef44c9b05e21e193a27c4d"
dependency_state: "satisfied"
durability_mode: "balanced"
redactions: 0
acceptance_state: "accepted"
history_file: "2026-09-02T12-29-28-162Z_e1a23da063ef44c9b05e21e193a27c4d.md"
---

# Request

> # PROMPT_HOST.md
>
> # SHARED NORMATIVE CONTRACT - VIBE DIRECT AUTH V1
>
> This section is normative and MUST be implemented identically by the client and host. Do not change field names, fingerprint encoding, HMAC input, state names, or security semantics on only one side.
>
> Protocol name: `Vibe Direct Auth v1`
>
> HTTPS API prefix: `/direct/v1`
>
> This protocol ONLY replaces onboarding/authentication, direct-host policy, trusted-device state, lightweight liveness, and diagnostics. It MUST NOT replace the existing GameStream video/audio/input/RTSP/media protocol.
>
> ## 1. Identities
>
> ### 1.1 Client identity
>
> Reuse Moonlight-Qt's existing persistent X.509 client certificate/private key from `IdentityManager`.
>
> DO NOT generate a second Ed25519/RSA identity for this project.
>
> The security identity is:
>
> `client_fingerprint = SPKI_SHA256(client_certificate)`
>
> SPKI SHA-256 algorithm:
> 1. Parse X.509.
> 2. Extract public key.
> 3. DER-encode SubjectPublicKeyInfo using `i2d_PUBKEY` or equivalent.
> 4. SHA-256 the DER bytes.
> 5. Encode the 32-byte digest using base64url with no `=` padding.
> 6. Wire representation MUST be `sha256/<base64url-no-padding>`.
>
> Device name, client UUID, IP, MAC, hostname, and certificate subject are metadata only and MUST NOT be used as trust keys.
>
> ### 1.2 Host identity
>
> Use the Vibepollo HTTPS server certificate public key.
>
> `host_fingerprint = SPKI_SHA256(host_server_certificate)`
>
> Use the same encoding as the client fingerprint.
>
> The client MUST pin this fingerprint. A changed fingerprint is an identity/security event, not an "offline" event.
>
> ### 1.3 Migration
>
> Existing Vibepollo `named_devices` certificates are already trusted. Compute their fingerprint on migration; do not re-pair them.
>
> Existing Moonlight host entries with a stored `serverCert` should seed the host fingerprint pin.
>
> ## 2. TLS and request authorization
>
> Use the existing HTTPS control plane and the existing Moonlight client certificate.
>
> Do not create custom encryption.
>
> Important: Direct Auth requires route-aware authorization AFTER TLS handshake. Unknown client certificates must be able to reach ONLY the minimal Direct Auth onboarding/status endpoints, while every sensitive GameStream endpoint remains trusted-client-only.
>
> Unknown client cert != trusted client.
>
> TLS handshake success != application authorization.
>
> ## 3. Capability advertisement
>
> The plain GameStream `/serverinfo` MAY advertise these non-security hints:
>
> ```xml
> <DirectAuthVersion>1</DirectAuthVersion>
> <DirectConnectSupported>1</DirectConnectSupported>
> <LegacyPinPairingEnabled>0</LegacyPinPairingEnabled>
> ```
>
> The client MUST treat them as hints only because plain HTTP can be spoofed. Host identity is established from the HTTPS certificate fingerprint.
>
> ## 4. Common JSON rules
>
> All Direct Auth responses are UTF-8 JSON and contain:
>
> ```json
> {"protocol": 1}
> ```
>
> Maximum Direct Auth request body: 16 KiB.
>
> Reject malformed JSON, unsupported versions, malformed fingerprints/base64url, unreasonable strings, and conflicting duplicate fields.
>
> Recommended machine-readable error envelope:
>
> ```json
> {
>   "protocol": 1,
>   "error": {
>     "code": "ENROLLMENT_CLOSED",
>     "message": "Enrollment is not open."
>   }
> }
> ```
>
> The client MUST key behavior on `code`, not localized English text.
>
> ## 5. `GET /direct/v1/status`
>
> Allowed for any syntactically valid presented client certificate, including unknown/blocked/revoked clients.
>
> It MUST NEVER create a host notification.
>
> Example:
>
> ```json
> {
>   "protocol": 1,
>   "host_name": "Gaming-PC",
>   "host_uuid": "host-stable-id",
>   "host_fingerprint": "sha256/...",
>   "client_fingerprint": "sha256/...",
>   "auth_state": "trusted",
>   "enrollment": {
>     "open": false,
>     "enrollment_id": null,
>     "expires_at_unix_ms": null
>   },
>   "legacy_pin_pairing_enabled": false
> }
> ```
>
> Allowed `auth_state`:
> - `trusted`
> - `unknown`
> - `blocked`
> - `revoked`
>
> Rules:
> - `client_fingerprint` is derived from the TLS peer cert.
> - `host_fingerprint` corresponds to the actual TLS server
> [truncated]

# Work Performed

- Executed a shell command.
- Executed tool `cbm_index_status`.
- Executed tool `cbm_get_architecture`.
- Executed tool `context_checkpoint`.
- Executed tool `cbm_search_graph`.
- Executed tool `rg`.
- Executed tool `ffgrep`.
- Inspected `.pi/memories/history/_active/2026-09-02T12-29-28-162Z_e1a23da063ef44c9b05e21e193a27c4d.md`.
- Executed tool `context_timeline`.
- Inspected `src/crypto.h`.
- Inspected `src/crypto.cpp`.
- Inspected `src/nvhttp.cpp`.
- Inspected `src/utility.h`.
- Executed tool `cbm_check_index_coverage`.
- Inspected `src/httpcommon.h`.
- Inspected `src/httpcommon.cpp`.
- Inspected `src/http_pairing_policy.h`.
- Inspected `src/http_pairing_policy.cpp`.
- Inspected `src/http_policy.h`.
- Inspected `src/http_policy.cpp`.
- Executed tool `todo_add`.
- Executed tool `todo_done`.
- Inspected `src/state_storage.h`.
- Executed tool `todo_next`.
- Executed tool `cbm_trace_path`; the tool reported an error.
- Inspected `src/rtsp.h`.
- Inspected `src/rtsp.cpp`.
- Inspected `src/stream.h`.
- Executed tool `context_compact`.

# Problems Encountered

- Tool `cbm_trace_path` reported an error.

# Outcome

The run ended, but the available observable evidence does not support claiming full task completion.
